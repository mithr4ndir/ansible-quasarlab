- name: Configure command center for k8s mgmt ansible modules
  hosts: cmd_center
  become: yes
  roles:
    - common/apps/kubectl
    - cmd_center
    - onepassword_cli
    - k8s/cni
    - k8s/argocd
