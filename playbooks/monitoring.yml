---
- name: Deploy monitoring agents to all Linux VMs
  hosts: linux
  become: true
  vars_files:
    - ../group_vars/vault.yml
  roles:
    - monitoring/node_exporter
    - role: monitoring/filebeat
      when: "'nas' not in group_names"
